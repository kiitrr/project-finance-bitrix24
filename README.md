# Project Finance — учёт финансов проектов для Bitrix24

Веб-приложение для ручного учета доходов и расходов по проектам с расчетом прибыли и рентабельности.

## Возможности

- список проектов и сводная финансовая эффективность;
- доходы и расходы по каждому проекту;
- стандартные статьи расходов: внешние программисты, внутренние программисты, расходы на ИИ, аренда сервера, дивиденды;
- добавление собственных статей доходов и расходов;
- добавление и удаление собственных статей;
- добавление сотрудников к проекту;
- создание, редактирование и удаление финансовых операций;
- поиск по операциям;
- автоматический расчёт дохода, расходов, прибыли и рентабельности;
- сохранение данных в `localStorage`;
- адаптивный интерфейс.

## Формулы

`Прибыль = Доходы − Расходы`

`Рентабельность = Прибыль / Расходы × 100%`

Если расходы равны 0, рентабельность отображается как 0%, чтобы избежать деления на ноль.

## Запуск

Требуется Node.js 18+.

```bash
npm install
npm run dev
```

Открыть адрес, который выведет Vite (обычно `http://localhost:5173`).

Для production-сборки:

```bash
npm run build
```

## Архитектура

- React + TypeScript + Vite
- Lucide React для иконок
- состояние приложения хранится в React state
- persistence — browser `localStorage`
- бизнес-логика расчётов отделена от представления на уровне функций `totals()` и `money()`

## Bitrix24

В рамках тестового задания реализовано веб-приложение для учета финансов по проектам.
Приложение работает независимо от конкретного портала Bitrix24, так как данные для подключения к REST API в задании не предоставлены. Поэтому в текущей версии реализована вся основная бизнес-логика: проекты, доходы, расходы, категории расходов, сотрудники, расчет прибыли и рентабельности.
Данные для демонстрации сохраняются в localStorage браузера.
При наличии доступа к конкретному порталу Bitrix24 приложение можно дополнительно подключить к REST API для загрузки и сохранения данных непосредственно в Bitrix24.
